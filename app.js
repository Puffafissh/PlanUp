let  availableDate = [   
        day,
        month,
        year,  
];
console.log(availableDate.length)

let  availableTime = [];
console.log(availableTime.length)

let  unavailableDate = [];
console.log(unavailableDate.length)

let  unavailableTime = [];
console.log(unavailableTime.length)

availableDate.reduce((accumulator, {day, month}))

availableTime.reduce((accumulator, {hours, minutes}) => {
    return accumulator + (min/60 + hours)
}, 0)

